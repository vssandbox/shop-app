export interface Deal {
  id: string;
  name: string;
  price: number;
  imageUrl: string;
}

export interface ShoppingListItem {
  id: string;
  name: string;
  price: number;
  imageUrl: string;
  addedAt: number;
}
